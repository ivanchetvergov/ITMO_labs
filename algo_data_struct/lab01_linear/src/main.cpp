#include "../tasks/A_stack_on_vector.hpp"
#include "../tasks/B_stack_on_list.hpp"
#include "../tasks/C_queue_on_vector.hpp"
#include "../tasks/D_queue_on_list.hpp"


int main(){

    // char choice;
    // cin >> choice;

    // if (choice == 'A') task_A();
    // else if (choice == 'B') task_B();
    // else if (choice == 'C') task_C();
    // else if (choice == 'D') task_D();

    task_D();

    return 0;
};